#!/usr/bin/env bash

##############################################
#	Author: Nidia Achrys
#	Project: debinstall
#	Version: 1.0
#
#	Usage: debinstall [command] [args]
#
#	Description:
#		Change this
##############################################

version='1.0'
author='Nidia Achrys'

# Source common script functions
source $SCRIPTS/libs/01-api.sh

#############
# Core Code #
#############

api.DEPENDENCY 'apt'

name="${1}"
name="${name##*/}"

if [[ ! "${name##*.}" == "deb" ]]; then

	api.FAIL "That is not a valid deb file!"

fi

api.PRINT 
api.PRINT 
api.LOG "Installing '${name}'..."
sudo apt install ${1} > /dev/null 2>&1

code=$?

[[ $code -eq 0 ]] && api.LOG "Installed '${name}'.\n" || api.FAIL "Install failed.\n"
