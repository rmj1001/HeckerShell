#!/usr/bin/env bash

##############################################
#	Author: Nidia Achrys
#	Project: debinstall
#	Version: 1.0
#
#	Usage: debinstall [command] [args]
#
#	Description:
#		Change this
##############################################

version='1.0'
author='Nidia Achrys'

# Source common script functions
source $SCRIPTS/01-api.sh

#############
# Core Code #
#############

DEPENDENCY 'apt'

name="${1}"
name="${name##*/}"

if [[ ! "${name##*.}" == "deb" ]]; then

	FAIL "That is not a valid deb file!"

fi

PRINT 
PRINT 
LOG "Installing '${name}'..."
sudo apt install ${1} > /dev/null 2>&1

code=$?

[[ $code -eq 0 ]] && LOG "Installed '${name}'.\n" || FAIL "Install failed.\n"
